# Core dependencies
torch>=2.0.0
transformers>=4.30.0
pillow>=9.0.0
pillow-heif>=0.10.0
ultralytics>=8.0.0
huggingface_hub>=0.16.0
depth-pro @ git+https://github.com/apple/ml-depth-pro.git

# Depth estimation
# Install via: git clone https://github.com/apple/ml-depth-pro && cd ml-depth-pro && pip install -e . && bash get_pretrained_models.sh
depth-pro @ git+https://github.com/apple/ml-depth-pro.git